# 🐛 Domain Grabber v3.7.1 - Bugfix Release

**Дата релізу:** 1 лютого 2026

---

## 🐛 Критичне виправлення

### Помилка копіювання

**Проблема:**
```
Uncaught (in promise) TypeError: Cannot set properties of null (setting 'innerText')
at content.js:1229
```

**Причина:**
- Функція `copyToClipboard()` намагалася встановити текст в елемент `statsMsg`
- Цей елемент був видалений в v3.7 разом з секцією "Results Area"
- Нотифікації не з'являлися після копіювання

**Рішення:**
```javascript
// БУЛО (v3.7) - поламано:
function copyToClipboard(text, msg) {
    navigator.clipboard.writeText(text).then(() => {
        const stats = document.getElementById('statsMsg'); // null!
        stats.innerText = msg; // ❌ Error!
    });
}

// СТАЛО (v3.7.1) - виправлено:
function copyToClipboard(text, msg) {
    navigator.clipboard.writeText(text).then(() => {
        showNotification(msg, 'success'); // ✅ Працює!
    }).catch(err => {
        showNotification('❌ Помилка копіювання!\n\n' + err.message, 'error');
    });
}
```

**Результат:**
- ✅ Нотифікації тепер з'являються коректно
- ✅ Додана обробка помилок копіювання
- ✅ Повідомлення показуються через `showNotification()`

---

## 📊 Тестування

### Перевірте що працює:

1. **JSON формат:**
   - Оберіть ◉ JSON
   - Натисніть "🎯 ЗІБРАТИ І СКОПІЮВАТИ"
   - Має з'явитися: `✅ JSON скопійовано! Знайдено доменів: X`

2. **Excel формат:**
   - Оберіть ◉ Excel
   - Натисніть "🎯 ЗІБРАТИ І СКОПІЮВАТИ"
   - Має з'явитися: `✅ Excel дані скопійовано! Знайдено доменів: X`

3. **Помилка:**
   - Якщо домени не знайдені: `❌ Домени не знайдені!`

---

## 🔄 Оновлення

### З v3.7 на v3.7.1:

```bash
cd domain-grabber-extensio
git pull origin main
```

Або завантажте ZIP: [v3.7.1 Release](https://github.com/gtailde/domain-grabber-extensio/releases/tag/v3.7.1)

### Перезавантаження розширення:

1. Відкрийте `chrome://extensions/`
2. Натисніть "🔄" біля Domain Grabber
3. Перезавантажте сторінку ExpiredDomains.net
4. Готово!

---

## 📝 Changelog

### v3.7.1 (Поточна)
🐛 **Bugfix:**
- Виправлено помилку копіювання (statsMsg is null)
- Додано обробку помилок в copyToClipboard()
- Нотифікації тепер працюють коректно

### v3.7
⚡ **Одноклікове копіювання:**
- Вибір формату JSON/Excel
- Кнопка "Зібрати і скопіювати"
- Автоматичне копіювання

---

## 🙏 Дякую за звіт про баг!

Якщо знайдете ще проблеми - повідомляйте!

**Happy domain hunting! 🚀**
