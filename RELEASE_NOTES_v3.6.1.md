# 🚀 Domain Grabber v3.6.1 - Release Notes

**Дата релізу:** 1 лютого 2026

---

## 🐛 Критичне виправлення

### Виправлено встановлення Results Limit

**Проблема:**
- Select поле Results Limit не встановлювалося правильно на ExpiredDomains.net
- Функція `setValue()` намагалася використовувати `selectedIndex` для select елементів
- ExpiredDomains.net очікує встановлення через властивість `.value`

**Рішення:**
- Спрощено логіку функції `setValue()`
- Видалено окрему обробку `select-one` через `selectedIndex`
- Тепер всі не-checkbox елементи (включно з select) встановлюються через `.value`

**Технічні деталі:**
```javascript
// ДО (неправильно):
const currentVal = el.type === 'checkbox' ? el.checked : (el.type === 'select-one' ? el.selectedIndex : el.value);
if (el.type === 'select-one') { el.selectedIndex = val; }

// ПІСЛЯ (правильно):
const currentVal = el.type === 'checkbox' ? el.checked : el.value;
el.value = val; // Працює для select, input, textarea
```

**Вплив:**
- ✅ Results Limit тепер коректно встановлюється на значення 200 (або інше вибране)
- ✅ Фільтр `flimit` правильно застосовується на ExpiredDomains.net
- ✅ Всі інші select поля також працюють коректно

---

## 📝 Зміни в коді

### content.js (Line ~1050-1070)
- **Видалено:** Окрему обробку `el.type === 'select-one'`
- **Спрощено:** Логіку перевірки поточного значення
- **Виправлено:** Метод встановлення значення для select елементів

---

## 📦 Як оновити

### Якщо ви використовуєте git:
```bash
git pull origin main
```

### Ручне оновлення:
1. Завантажте v3.6.1 з [GitHub Releases](https://github.com/gtailde/domain-grabber-extensio/releases)
2. Видаліть стару версію через `chrome://extensions/`
3. Встановіть нову версію через **Load unpacked**

### Перевірка:
1. Відкрийте розширення на ExpiredDomains.net
2. Натисніть **⚡ Встановити фільтри**
3. Перевірте, що поле Results Limit встановилось на 200 (або інше вибране значення)
4. Застосуйте фільтри на сайті та перевірте кількість результатів

---

## 🔗 Повна історія змін

- **v3.6.1** - Виправлено встановлення Results Limit через select
- **v3.6** - DROP режим за замовчуванням, Block TLDs увімкнено, Results Limit 200
- **v3.5** - Автозаповнення фільтрів, покращений UI
- **v3.2** - Додано підтримку метрик CF/TF/DP
- **v3.1** - Початкова версія

---

## 🙏 Подяки

Дякуємо користувачам за звіти про баги! Цей реліз виправляє критичну помилку, яка заважала правильному встановленню фільтрів.

**Якщо знайдете ще баги - повідомляйте!** 🐛
