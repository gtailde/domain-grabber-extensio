# 🐛 Domain Grabber v3.7.3 - Bugfix Release

**Дата релізу:** 1 лютого 2026

---

## 🐛 Виправлення критичного бага

### Проблема

При переходах по сайту (пагінація, сортування, фільтри) на ExpiredDomains.net:
- ❌ Панель автоматично відкривалась якщо була відкрита раніше
- ❌ Це відбувалось при кожному переході на нову сторінку
- ❌ Заважало нормальній навігації по сайту
- ❌ Користувач втрачав контроль над розширенням

**Репродукція:**
1. Відкрити панель Domain Grabber (🚀 Grab)
2. Перейти на наступну сторінку (пагінація)
3. Панель автоматично відкривалась знову ❌

---

## ✅ Що виправлено

### Прибрано автовідкриття панелі

**Було (v3.7.2):**
```javascript
// Перевіряємо чи панель була відкрита раніше
const wasPanelOpen = localStorage.getItem('domain-grabber-panel-open') === 'true';
if (wasPanelOpen) {
    showMainUI(); // ❌ Автоматично відкриває панель
}
```

**Стало (v3.7.3):**
```javascript
// Click handler
launchBtn.addEventListener('click', function(e) {
    e.preventDefault();
    e.stopPropagation();
    showMainUI(); // ✅ Відкривається тільки при натисканні кнопки
});
```

---

## 🎯 Нова поведінка

### Повний контроль користувача

**Панель відкривається:**
- ✅ Тільки при натисканні кнопки **🚀 Grab**
- ✅ Коли ви самі вирішите її відкрити
- ✅ Ніколи автоматично при переходах

**Фільтри застосовуються:**
- ✅ Тільки при натисканні **⚡ Встановити фільтри**
- ✅ Не застосовуються автоматично
- ✅ Повний контроль над процесом

**Навігація:**
- ✅ Можна вільно переходити по сторінках
- ✅ Користуватись пагінацією
- ✅ Сортувати результати
- ✅ Без небажаних сюрпризів

---

## 📊 Порівняння

### Було (v3.7.2):
```
1. Відкриваєте панель 🚀 Grab
2. Переходите на наступну сторінку →
3. ❌ Панель автоматично відкривається знову
4. ❌ Заважає переглядати результати
5. ❌ Доводиться постійно закривати
```

### Стало (v3.7.3):
```
1. Відкриваєте панель 🚀 Grab
2. Переходите на наступну сторінку →
3. ✅ Панель не відкривається автоматично
4. ✅ Спокійно переглядаєте результати
5. ✅ Відкриваєте панель коли потрібно
```

---

## 🔧 Технічні деталі

### Зміни в коді

**Файл:** `content.js`

**Видалено (рядки 43-47):**
```javascript
// Перевіряємо чи панель була відкрита раніше
const wasPanelOpen = localStorage.getItem('domain-grabber-panel-open') === 'true';
if (wasPanelOpen) {
    showMainUI();
}
```

**Причина:**
- Content script перезапускається при кожному переході на нову сторінку
- `init()` викликається заново
- Якщо `localStorage` містив `domain-grabber-panel-open: 'true'`
- Панель автоматично відкривалась через `showMainUI()`

**Рішення:**
- Прибрав автоматичне відновлення стану панелі
- Панель тепер відкривається тільки вручну
- Зберігається позиція панелі (v3.7.2)
- Але не зберігається стан відкрита/закрита

---

## 🚀 Оновлення

### З v3.7.2 на v3.7.3:

**Спосіб 1 - Git:**
```bash
cd domain-grabber-extensio
git pull origin main
```

**Спосіб 2 - ZIP:**
Завантажте [v3.7.3 Release](https://github.com/gtailde/domain-grabber-extensio/releases/tag/v3.7.3)

**Спосіб 3 - Ручне оновлення:**
1. Відкрийте `chrome://extensions/`
2. Знайдіть Domain Grabber
3. Натисніть 🔄 (Reload)
4. Перезавантажте сторінку ExpiredDomains.net

---

## ✅ Тестування

### Як перевірити що баг виправлено:

1. Відкрийте ExpiredDomains.net
2. Натисніть **🚀 Grab** - панель відкриється ✅
3. Закрийте панель (✖️)
4. Перейдіть на наступну сторінку (пагінація)
5. **Панель НЕ повинна відкритись автоматично** ✅
6. Натисніть **🚀 Grab** знову - панель відкриється ✅

**Очікуваний результат:**
- Панель відкривається тільки при натисканні кнопки
- Навігація по сайту не відкриває панель
- Повний контроль над розширенням

---

## 📝 Повний Changelog

### v3.7.3 (Поточна)
🐛 **Bugfix:**
- Виправлено автовідкриття панелі при переходах
- Панель відкривається тільки при натисканні 🚀 Grab
- Фільтри застосовуються тільки при натисканні ⚡ Встановити фільтри
- Стабільна навігація по сайту

### v3.7.2
✨ **UX покращення:**
- Scroll для панелі
- Запам'ятовування позиції

### v3.7.1
🐛 **Bugfix:**
- Виправлено copyToClipboard()

### v3.7
⚡ **Одноклікове копіювання:**
- Вибір формату JSON/Excel
- Автокопіювання в буфер

---

## 💡 Рекомендації

### Типовий workflow з v3.7.3:

1. **Налаштування фільтрів:**
   - Натисніть 🚀 Grab
   - Відкрийте секцію "🎯 Автозаповнення фільтрів"
   - Налаштуйте: ACR >= 1, Available, Limit 200, Block TLDs
   - Натисніть **⚡ Встановити фільтри**
   - Закрийте панель (✖️)

2. **Пошук доменів:**
   - Переглядайте результати
   - Переходьте по сторінках (пагінація)
   - Панель не заважатиме

3. **Збір даних:**
   - Коли знайдете потрібні домени
   - Натисніть 🚀 Grab
   - Виберіть формат (JSON/Excel)
   - Натисніть **🎯 ЗІБРАТИ І СКОПІЮВАТИ**
   - Готово! Дані в буфері обміну

---

## 🙏 Дякуємо за відгук!

Це виправлення було зроблено завдяки вашому відгуку:
> "найшов баг коли натискаю перейти за посиланням js сам постійно запускається так не можна має сортувати і добавляти тільки коли я нажму кнопку застосувати"

**Продовжуйте повідомляти про проблеми!** 🐛

---

**Happy domain hunting! 🚀**

**Made with ❤️ for domain hunters**
