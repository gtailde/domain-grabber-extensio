# 🚀 Domain Grabber v3.7.2 - Release Notes

**Дата релізу:** 1 лютого 2026

---

## ✨ UX покращення

### 📜 Scroll для панелі

**Проблема:**
- При відкритті всіх секцій (автофільтри, метрики, фільтри) панель виходила за межі екрану
- Не було можливості натиснути кнопку "Зібрати і скопіювати" внизу
- Доводилося згортати секції або змінювати масштаб

**Рішення:**
```css
#grabber-ui {
    max-height: calc(100vh - 40px);  /* Не більше висоти екрану */
    overflow-y: auto;                 /* Вертикальний scroll */
}
```

**Стилізований scrollbar:**
- Ширина: 8px
- Колір: темний (#475569)
- Hover ефект: світліше (#64748b)
- Закруглені краї для естетики

**Результат:**
- ✅ Панель завжди вміщується на екрані
- ✅ Scroll з'являється автоматично при потребі
- ✅ Красивий дизайн скролбару в темній темі

---

### 💾 Запам'ятовування позиції панелі

**Проблема:**
- При відкритті нової сторінки панель завжди з'являлася справа вгорі
- Доводилося кожного разу переміщати її в зручне місце
- Незручно при частому використанні

**Рішення:**
```javascript
// При відкритті панелі - відновлення позиції:
const savedPosition = localStorage.getItem('domain-grabber-position');
// { top: "100px", left: "200px" }

// При закритті drag - збереження позиції:
function closeDragElement() {
    const position = {
        top: element.style.top,
        left: element.style.left
    };
    localStorage.setItem('domain-grabber-position', JSON.stringify(position));
}
```

**Результат:**
- ✅ Панель відкривається там де ви її залишили
- ✅ Позиція зберігається між сесіями
- ✅ Працює на всіх вкладках ExpiredDomains.net
- ✅ Не потрібно постійно переміщати панель

---

## 🎨 Візуальні зміни

### Scrollbar дизайн

```css
/* Ширина scrollbar */
#grabber-ui::-webkit-scrollbar {
    width: 8px;
}

/* Трек (фон) */
#grabber-ui::-webkit-scrollbar-track {
    background: #1e293b;
    border-radius: 12px;
}

/* Thumb (повзунок) */
#grabber-ui::-webkit-scrollbar-thumb {
    background: #475569;
    border-radius: 4px;
}

/* Hover ефект */
#grabber-ui::-webkit-scrollbar-thumb:hover {
    background: #64748b;
}
```

---

## 🔧 Технічні деталі

### localStorage ключі:

**Існуючі:**
- `domain-grabber-panel-open` - стан панелі (відкрита/закрита)
- `domain-grabber-settings` - налаштування (режим, метрики, фільтри)
- `domain-grabber-filters` - налаштування автофільтрів
- `domain-grabber-autofilters-collapsed` - стан згортання секції автофільтрів

**Новий:**
- `domain-grabber-position` - позиція панелі `{ top: "100px", left: "200px" }`

### Приклад збереженої позиції:

```json
{
  "top": "150px",
  "left": "300px"
}
```

---

## 📊 Переваги v3.7.2

### Було (v3.7.1):
- ❌ Панель виходила за екран при відкритті всіх секцій
- ❌ Кнопки внизу були недоступні
- ❌ Завжди відкривалася справа вгорі
- ❌ Доводилося кожного разу переміщати

### Стало (v3.7.2):
- ✅ Scroll автоматично з'являється при потребі
- ✅ Всі кнопки завжди доступні
- ✅ Відкривається в останній позиції
- ✅ Налаштували один раз - працює завжди

---

## 🔄 Оновлення

### З v3.7.1 на v3.7.2:

```bash
cd domain-grabber-extensio
git pull origin main
```

Або завантажте ZIP: [v3.7.2 Release](https://github.com/gtailde/domain-grabber-extensio/releases/tag/v3.7.2)

### Перезавантаження розширення:

1. Відкрийте `chrome://extensions/`
2. Натисніть "🔄" біля Domain Grabber
3. Перезавантажте сторінку ExpiredDomains.net
4. Панель відкриється в останній позиції!

---

## 📝 Повний Changelog

### v3.7.2 (Поточна)
✨ **UX покращення:**
- Додано scroll для панелі (max-height + overflow-y)
- Стилізований темний scrollbar (8px, hover ефект)
- Запам'ятовування позиції панелі через localStorage
- Відновлення позиції при відкритті нової сторінки

### v3.7.1
🐛 **Bugfix:**
- Виправлено copyToClipboard() помилку
- Нотифікації тепер працюють коректно

### v3.7
⚡ **Одноклікове копіювання:**
- Вибір формату JSON/Excel
- Кнопка "Зібрати і скопіювати"
- Автоматичне копіювання в буфер

---

## 💡 Поради по використанню

### Як налаштувати ідеальну позицію:

1. Відкрийте панель Domain Grabber
2. Перемістіть її в зручне місце (наприклад, ліворуч або по центру)
3. Закрийте панель
4. При наступному відкритті вона буде саме там! 🎯

### Якщо хочете скинути позицію:

**Варіант 1 - через DevTools:**
```javascript
// Відкрийте консоль (F12) на ExpiredDomains.net
localStorage.removeItem('domain-grabber-position');
// Панель повернеться в дефолтну позицію (справа вгорі)
```

**Варіант 2 - просто перемістіть:**
- Перемістіть панель в нове місце
- Нова позиція автоматично збережеться

---

## 🎁 Бонус

### Зручні комбінації позицій:

**Для широких екранів:**
- Ліворуч: `{ top: "20px", left: "20px" }`
- Праворуч: `{ top: "20px", right: "20px" }` (дефолт)
- По центру: `{ top: "20px", left: "50%", transform: "translateX(-50%)" }`

**Для вертикальних екранів:**
- Згори: `{ top: "20px", right: "20px" }`
- Знизу: `{ bottom: "20px", right: "20px" }`

---

## 🙏 Дякуємо за відгуки!

Ці покращення були реалізовані на основі ваших пропозицій:
- "Додай scroll бо буває панель виходить за екран" ✅
- "Зроби щоб появлялось там де залишив" ✅

**Продовжуйте ділитися ідеями!** 💡

---

**Happy domain hunting! 🚀**
