# 🚀 Domain Grabber v3.6 - Release Notes

**Дата релізу:** 1 лютого 2026

---

## 🎯 Основні зміни

### ✨ Нові можливості

1. **💧 DROP режим за замовчуванням**
   - При першому запуску активується режим DROP
   - Більшість користувачів працюють саме з DROP доменами
   - Режим BRANDS залишається доступним одним кліком

2. **🚫 Block TLDs увімкнено за замовчуванням**
   - Автоматичне блокування 629 небажаних доменних зон
   - Список повністю редагується
   - Можна вимкнути одним чекбоксом

3. **📊 Results Limit 200 за замовчуванням**
   - Оптимальна кількість результатів для більшості задач
   - Доступні варіанти: 25, 50, 75, 100, 200

### 🐛 Виправлення помилок

1. **Баг з множинними попапами**
   - Виправлено: при повторному натисканні кнопок створювалось декілька нотифікацій
   - Тепер старі попапи автоматично видаляються перед показом нового
   - Додано клас `.grabber-notification` для ідентифікації

### 🎨 Покращення UI

1. **Збільшено кнопки**
   - Кнопка "⚡ Встановити фільтри": padding 12px, font-size 13px
   - Кнопка "🔄 Скинути налаштування": padding 10px, font-size 12px
   - Покращена читабельність та зручність використання

2. **Оптимізовано відступи**
   - Збільшено margin-top для кнопок
   - Покращено візуальну ієрархію

---

## 📝 Повний список змін

### content.js
- Додано видалення попередніх нотифікацій в `showNotification()`
- Змінено дефолтний режим з 'brands' на 'drop'
- Додано else блок для правильного відображення режиму BRANDS
- Увімкнено `blackzone_enable: true` за замовчуванням
- Змінено дефолтний `limit_value` на '200'
- Збільшено padding кнопок автофільтрів

### README.md
- Оновлено версію до 3.6
- Додано опис нових можливостей v3.6
- Оновлено дефолтні налаштування в документації
- Уточнено режим за замовчуванням (DROP)

---

## 🔧 Технічні деталі

### Дефолтні налаштування фільтрів:
```javascript
{
  acr_enable: true,
  acr_value: '1',
  available_enable: true,
  limit_enable: true,
  limit_value: '200',           // ← змінено з '25'
  exclude_makeoffer_enable: true,
  period: 'flast168',
  contains_enable: false,
  contains_list: '26 gambling слів',
  blackzone_enable: true,       // ← змінено з false
  blackzone_list: '629 TLD зон'
}
```

### Дефолтний режим:
```javascript
currentMode = savedSettings?.mode || 'drop'  // ← змінено з 'brands'
```

---

## 📦 Установка

### Оновлення з попередньої версії:
1. Видаліть попередню версію розширення
2. Завантажте v3.6 з GitHub
3. Встановіть нову версію через `chrome://extensions/`
4. При першому запуску будуть застосовані нові дефолтні налаштування

### Нова установка:
```bash
git clone https://github.com/gtailde/domain-grabber-extensio.git
cd domain-grabber-extensio
```

Потім завантажте в Chrome через Developer Mode.

---

## 🎯 Рекомендації по використанню

### Для DROP доменів (дефолт):
1. Відкрийте панель (🚀 Grab)
2. Режим DROP вже активний
3. Налаштуйте метрики (BL, ACR, CF, TF, DP)
4. За бажанням змініть фільтри автозаповнення
5. Натисніть "⚡ Встановити фільтри"
6. Зберіть домени кнопкою "🎯 ЗІБРАТИ"

### Для BRANDS доменів:
1. Переключіться на режим BRANDS
2. Введіть список брендів (nike, adidas, puma...)
3. Налаштуйте метрики
4. Застосуйте фільтри
5. Зберіть домени

### Скидання налаштувань:
- Натисніть "🔄 Скинути налаштування"
- Підтвердіть дію
- Всі фільтри повернуться до дефолтних значень v3.6

---

## 🔗 Посилання

- **GitHub:** https://github.com/gtailde/domain-grabber-extensio
- **Issues:** https://github.com/gtailde/domain-grabber-extensio/issues
- **Releases:** https://github.com/gtailde/domain-grabber-extensio/releases

---

## 📊 Статистика версії

- **Файлів змінено:** 2 (content.js, README.md)
- **Додано рядків:** ~45
- **Видалено рядків:** ~15
- **Виправлено багів:** 1 (множинні попапи)
- **Нових можливостей:** 3

---

## 💡 Що далі?

У наступних версіях планується:
- Експорт у CSV формат
- Додаткові метрики (Moz DA/PA)
- Збереження пресетів фільтрів
- Історія зібраних доменів
- Інтеграція з API ExpiredDomains

---

**Дякуємо за використання Domain Grabber!** 🎉
